{
  "skill_id": "yao-geoflow-cli",
  "required_files": [
    "SKILL.md",
    "agents/interface.yaml",
    "evals/trigger_cases.json",
    "evals/expected_artifacts.json",
    "templates/brief-template.md",
    "scripts/geoflow_preflight.sh"
  ],
  "primary_outputs": [
    "catalog-lookup-result",
    "material-operation-result",
    "task-operation-result",
    "article-publish-result"
  ],
  "quality_expectations": [
    "must operate through the local geoflow CLI when it exists and supports the action",
    "must use Laravel API v1 fallback when the current workspace has no bin/geoflow wrapper",
    "must not bypass the system with direct database writes",
    "must verify preflight before mutating operations",
    "must verify materials scope before material writes",
    "must report exact CLI/API and system state changes",
    "must not claim admin-only distribution management is exposed through API v1"
  ]
}
