# yao-geo-execution-roadmap Review - 2026-05-19

## Review Scope

- Skill routing and boundaries.
- Required package files, interface, manifest, docs and registry consistency.
- Renderer behavior for Markdown, HTML, Word and PDF.
- HubSpot and Lingxu example existence and quality reports.
- Word table overflow risk, especially right-side overflow.

## Findings And Fixes

| Finding | Fix |
|---|---|
| Expected artifact list did not require `quality-report.json` for examples. | Added quality reports for both HubSpot and Lingxu demos to `evals/expected_artifacts.json`. |
| Interface guardrails did not explicitly include Word fixed table geometry. | Added `require_docx_fixed_table_geometry` and `split_wide_word_tables_into_grouped_tables`. |
| Trigger evals did not include a four-format + Word overflow scenario. | Added a trigger case for Word/PDF/HTML/Markdown generation and Word overflow review. |
| DOCX renderer previously used auto-width tables. | Renderer now emits fixed page width, fixed `tblGrid`, fixed `tcW`, fixed table layout and grouped narrow tables. |

## Verification

- HubSpot example quality: pass.
- Lingxu example quality: pass.
- HubSpot DOCX: 35 tables, 0 auto-width tables, 35 `tblGrid`, 495 `tcW`, max table width 9638 DXA.
- Lingxu DOCX: 29 tables, 0 auto-width tables, 29 `tblGrid`, 423 `tcW`, max table width 9638 DXA.
- HubSpot DOCX rendered through Pages to PDF: 10 pages, minimum detected right margin 119 px.
- HubSpot native PDF: 11 pages, minimum detected right margin 84 px.

## Remaining Notes

- Repository-level validation is still blocked by an unrelated existing registry entry: `yao-geo-content-refiner` is missing `SKILL.md`.
- This skill's focused audit passes.
